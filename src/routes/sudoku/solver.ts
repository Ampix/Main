import type { digitType, solverOutput } from './types.ts';

function getPossibleDigits() {}

function solve(grid: digitType[]): solverOutput {
	let output: solverOutput = { possibilities: 0, solved: grid };

	return output;
}
